server_addr: "mail.heclouds.com:10926"
trust_host_root_certs: false
tunnels:
  webhook:
    proto:
      http: 5000
  # 固定端口号
  # socket:
  #   remote_port: 54812
  #   proto:
  #     tcp: 127.0.0.1:9501

# Example. Add by boer.
#
# Eg.1
# ./ngrok -config=ngrok.cfg -subdomain=webhook 5000
# ./<对应自己系统的ngrok> -config=<下载的配置文件> -subdomain=<自定义子域名> <本地需要映射的端口>
#
# Eg.2
# ./ngrok -config=ngrok.cfg start webhook
